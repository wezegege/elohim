[messages]
playerturn={{ display_all('score::permanent') }}
           {{ gettext('playerturn %(name)s', name='players::current::name') }}
round={{ display_one('score::permanent', 'score::temporary')
winner={{ display_all('score::permanent') }}
        {{ gettext('winner %(name)s', name=name) }}
roll={{ gettext('roll %(name)s %(roll)d', name=name, roll=roll) }}
choice={{ gettext('choice %(name)s %(choice)s'n, name=name, choice=choice) }}

[options]
hold=Hold score
roll=Roll a dice
